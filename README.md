# Mini DevOps Project

Simple Python app containerized with Docker.

## Build

docker build -t mini-devops .

## Run

docker run mini-devops

## CI

This project uses GitHub Actions to automatically build the Docker image on every push.
